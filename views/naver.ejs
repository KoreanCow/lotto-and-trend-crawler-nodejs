<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <div>
        <!-- test -->
        <!-- <%= title %> -->
        <!-- <%= title[1].title %> -->
        <form action="/">
          <button type="submit">메인 메뉴</button>
        </form>
        <ol>
            <% for(let i = 0; i < title.length; i++){%>
              <li>
                <%= title[i].title %>
              </li>
              <%}%>
        </ol>
      </div>
  </body>
</html>
